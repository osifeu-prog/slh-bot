# Alpha UI readiness

The read-only Alpha Status card is specified against `data.alpha` from `/api/v1/me`.

No allocation, token transfer, balance mutation, financial transaction, deploy, or main merge is performed by this work.
